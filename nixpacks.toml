[phases.setup]
nixPkgs = ["php83", "php83Extensions.bcmath", "php83Extensions.gd", "php83Extensions.pdo", "php83Extensions.pdo_mysql", "php83Extensions.zip", "php83Extensions.mbstring", "php83Extensions.posix", "nodejs-18_x", "yarn", "nginx", "netcat", "curl", "mysql80"]

[phases.install]
cmds = [
    "composer install --no-dev --optimize-autoloader --no-interaction",
    "yarn install --frozen-lockfile"
]

[phases.build]
cmds = [
    "yarn run build:production"
]

[start]
cmd = "sh railway-start.sh"
